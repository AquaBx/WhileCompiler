package org.esir.nctt.while_compiler.Visitor.IntermediateCode;

public enum InstructionType {
    Pop,
    Push,
    Define,
    Mov,
    SetHead,
    SetTail,
    Call,
    If,
    Else,
    Endif,
    Goto,
    Inc
}

