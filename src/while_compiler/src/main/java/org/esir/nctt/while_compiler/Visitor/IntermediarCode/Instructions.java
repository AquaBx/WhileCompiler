package org.esir.nctt.while_compiler.Visitor.IntermediarCode;

public enum Instructions {
    Pop,
    Push,
    Define,
    Mov,
    SetHead,
    SetTail,
    Call,
    If,
    Else,
    Endif,
    Goto,
    Inc
}

